.space-y-3
  - @keywords.each do |label|
    - keyword_value = "#{label.start_location_name}_#{label.end_location_name}"
    label.flex.items-center.space-x-2
      = check_box_tag "keywords[]", keyword_value, params[:keywords]&.include?(keyword_value), class: 'checkbox checkbox-primary', data: { filter: 'keywords' }
      span #{label.start_location_name} → #{label.end_location_name}
